# Extracción de documentos
pymupdf>=1.24
pymupdf4llm>=1.0
beautifulsoup4>=4.12
markdownify>=0.11
python-docx>=1.1
lxml>=5.0

# Voz neuronal gratuita (Microsoft Edge TTS)
edge-tts>=7.0

# Generación de imágenes del video
Pillow>=10.0
pysrt>=1.1

# Dependencias del sistema Python
pydantic-settings>=2.0

# Requisitos del sistema (no pip):
#   - ffmpeg        (ensamblar el video)
#   - libreoffice   (convertir .doc antiguos a .docx)
